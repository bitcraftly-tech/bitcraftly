/**
 * Page gutter for the Ecommerce Store showcase.
 *
 * Full-bleed (no max-width) so the storefront fills the viewport, with an 8px
 * rail on phones and 32px from the `sm` breakpoint up.
 */
export const EC_CONTAINER = 'mx-auto w-full px-2 sm:px-8';
