/**
 * Resources segment layout — supports /resources and nested routes such as /resources/faq.
 */
export default function ResourcesLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return children;
}
