/**
 * Stub: legacy portfolio “why we perform” block. Full marketing portfolio page redirects to /work.
 * Kept so unused PortfolioShowcaseSection still typechecks.
 */
type PortfolioWhyPerformProps = {
  light?: boolean;
};

export default function PortfolioWhyPerform(_props: PortfolioWhyPerformProps) {
  return null;
}
